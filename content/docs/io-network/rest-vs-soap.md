---
title: "REST vs SOAP"
description: "So sánh hai phong cách API: REST (resource, HTTP verb, JSON) và SOAP (protocol, XML, WSDL) — khi nào dùng cái nào."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

So sánh hai phong cách API: REST (resource, HTTP verb, JSON) và SOAP (protocol, XML, WSDL) — khi nào dùng cái nào.

## Nội dung sẽ bao gồm

- REST — nguyên tắc, HTTP verb, status
- SOAP — envelope, WSDL, WS-*
- Định dạng: JSON vs XML
- Stateless & caching
- Khi nào dùng REST vs SOAP
