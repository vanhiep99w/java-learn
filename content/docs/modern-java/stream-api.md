---
title: "Stream API"
description: "Stream pipeline trong Java 8: intermediate vs terminal operation, lazy evaluation, collector và parallel stream."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Stream pipeline trong Java 8: intermediate vs terminal operation, lazy evaluation, collector và parallel stream.

## Nội dung sẽ bao gồm

- Stream vs Collection
- Intermediate (map/filter) vs Terminal (collect/reduce)
- Lazy evaluation & short-circuit
- Collectors (groupingBy, toMap)
- Parallel stream — khi nào nên dùng
