---
title: "Pattern Matching & Switch Expressions"
description: "Switch expression (Java 14), pattern matching cho instanceof và switch (Java 21) giúp code ngắn gọn, an toàn hơn."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Switch expression (Java 14), pattern matching cho instanceof và switch (Java 21) giúp code ngắn gọn, an toàn hơn.

## Nội dung sẽ bao gồm

- Switch expression (arrow, yield)
- Pattern matching for instanceof
- Pattern matching for switch
- Record pattern (destructuring)
- Exhaustiveness với sealed type
