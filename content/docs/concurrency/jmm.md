---
title: "Java Memory Model (JMM)"
description: "Mô hình bộ nhớ Java: visibility, ordering, atomicity, quan hệ happens-before, reordering và memory barrier."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Mô hình bộ nhớ Java: visibility, ordering, atomicity, quan hệ happens-before, reordering và memory barrier.

## Nội dung sẽ bao gồm

- Vấn đề: visibility / ordering / atomicity
- Happens-before relationship
- Reordering (compiler, JIT, CPU)
- Memory barrier
- Safe publication
