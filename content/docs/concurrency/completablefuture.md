---
title: "CompletableFuture & Async"
description: "Lập trình bất đồng bộ với CompletableFuture: compose, combine, xử lý exception và chỉ định executor."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Lập trình bất đồng bộ với CompletableFuture: compose, combine, xử lý exception và chỉ định executor.

## Nội dung sẽ bao gồm

- Future vs CompletableFuture
- thenApply / thenCompose / thenCombine
- Xử lý exception (exceptionally, handle)
- allOf / anyOf
- Chỉ định Executor & cạm bẫy common pool
