---
title: "Thread vs Runnable & ExecutorService"
description: "Cách tạo thread (extends Thread vs implements Runnable/Callable) và quản lý thread bằng ExecutorService/thread pool."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Cách tạo thread (extends Thread vs implements Runnable/Callable) và quản lý thread bằng ExecutorService/thread pool.

## Nội dung sẽ bao gồm

- Thread vs Runnable vs Callable
- Vòng đời & trạng thái thread
- ExecutorService & thread pool
- Future & Callable
- Vì sao không nên tạo thread thủ công
