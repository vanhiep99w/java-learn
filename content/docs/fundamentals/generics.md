---
title: "Generics & Type Erasure"
description: "Generics trong Java, bounded type, wildcard (? extends / ? super), và cơ chế type erasure khi compile."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Generics trong Java, bounded type, wildcard (? extends / ? super), và cơ chế type erasure khi compile.

## Nội dung sẽ bao gồm

- Generic class, method, interface
- Bounded type parameter
- Wildcard: extends vs super (PECS)
- Type Erasure — điều gì xảy ra lúc compile
- Giới hạn của generics (no new T[])
