---
title: "String, String Pool & StringBuilder"
description: "Tại sao String immutable, cơ chế String Pool (intern), so sánh String vs StringBuilder vs StringBuffer."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Tại sao String immutable, cơ chế String Pool (intern), so sánh String vs StringBuilder vs StringBuffer.

## Nội dung sẽ bao gồm

- String immutable — lý do & lợi ích
- String Pool & intern()
- == vs equals() với String
- StringBuilder vs StringBuffer
- Hiệu năng nối chuỗi trong vòng lặp
