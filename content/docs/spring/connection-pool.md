---
title: "Connection Pool (HikariCP)"
description: "Vì sao cần connection pool, cách hoạt động, và tuning HikariCP (pool size, timeout) để tránh exhaust."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Vì sao cần connection pool, cách hoạt động, và tuning HikariCP (pool size, timeout) để tránh exhaust.

## Nội dung sẽ bao gồm

- Vì sao cần pooling
- Vòng đời connection trong pool
- HikariCP & cấu hình
- maximumPoolSize & công thức tính
- Connection leak & timeout
