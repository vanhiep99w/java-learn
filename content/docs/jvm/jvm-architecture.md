---
title: "Kiến trúc JVM & Class Loading"
description: "Các thành phần của JVM: ClassLoader, Runtime Data Areas, Execution Engine và cơ chế class loading (delegation model)."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Các thành phần của JVM: ClassLoader, Runtime Data Areas, Execution Engine và cơ chế class loading (delegation model).

## Nội dung sẽ bao gồm

- ClassLoader: Bootstrap / Extension / Application
- Parent delegation model
- Runtime Data Areas (Heap, Stack, Metaspace, PC)
- Execution Engine & JIT
- Loading → Linking → Initialization
