---
title: "JVM, JDK & JRE"
description: "Phân biệt JVM, JRE, JDK; quá trình từ source .java tới bytecode .class và cách JVM thực thi."
---

> [!NOTE]
> Đây là bản nháp (placeholder). Nội dung chi tiết sẽ được bổ sung sau — phần dưới là dàn ý các đề mục dự kiến.

## Tổng quan

Phân biệt JVM, JRE, JDK; quá trình từ source .java tới bytecode .class và cách JVM thực thi.

## Nội dung sẽ bao gồm

- JDK = JRE + tools (javac, jdb...)
- JRE = JVM + thư viện runtime
- JVM — máy ảo thực thi bytecode
- Quy trình compile & run
- JIT vs Interpreter
